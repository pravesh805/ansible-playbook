---
- name: Deploy to K8s Cluster
  hosts: localhost
  become: ubuntu
  tasks:  # Added hyphen before "tasks"
    - name: Deploy Pod
      shell: |
        kubectl apply -f deployment.yml

    - name: Deploy Service
      shell: |
        kubectl apply -f pod.yml
